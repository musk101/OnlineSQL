import React from "react";
import "./Header.css";
import Grid, { GridSpacing } from "@material-ui/core/Grid";

export default function Header() {
  return (
    <div>
      <p className="header">SQL Editor</p>
    </div>
  );
}
